export const environment = {
  production: false,
  // Use Angular dev-server proxy to avoid browser CORS preflight failures.
  // Relative URL so requests go through proxy.conf.json
  apiBaseUrl: ''
};
